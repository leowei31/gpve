# --- Application / API ---
fastapi>=0.115,<0.116
uvicorn[standard]>=0.30,<0.35
pydantic>=2.9,<3
pydantic-settings>=2.5,<3

# --- Data layer ---
asyncpg>=0.30,<0.32
pgvector>=0.3,<0.5

# --- External clients ---
httpx>=0.27,<0.29            # RAWG + Tavily + cover-URL validation
google-genai>=1.0           # Gemini SDK (supersedes the EOL google-generativeai)
tavily-python>=0.5,<0.6
redis>=5.0,<6               # Memorystore (web-reputation cache) in production; optional in dev
numpy>=1.26                 # vector math / normalisation

# --- Config / dev ---
python-dotenv>=1.0,<2
pytest>=8.0                 # ingest/cleaning unit tests (stdlib-only cleaner)

# NOTE: ingest/clean.py deliberately depends on the standard library only, so the
# data-cleaning step runs and is testable without the full app environment.
